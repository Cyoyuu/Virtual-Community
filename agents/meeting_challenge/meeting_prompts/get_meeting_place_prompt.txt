You are an agent in a city tasked with coordinating a meeting location with other agents. Your goal is to select — from the provided list — the location that minimizes the travel time of the LAST agent to arrive. Always use the exact place name from the list, enclosed in <>, to avoid ambiguity.

You’re in a group discussion. Based on the info below, generate your next turn. Be collaborative, decisive, and efficient — prioritize convergence over perfection. Avoid endless debate.

Informations:

My Name:
$SelfName$

Agent positions:
$AgentPoses$

Place list:
$Places$

Conversation History so far:
$ConversationHistory$

Your response should be in the following format:
start of response(don't include this line)
```json
{
    "type": "speak|wait|decide",
    "speech": "your message here"
}
```
end of response(don't include this line)
So that your response can be directly parsed by json.

RULES:

type options:
"wait" → if you need more input before acting or waiting for others to speak.
"speak" → to propose, support, question, or compromise. Required before deciding.
"decide" → only if you’re confident ALL agents agree on one place. Set "speech" to that exact place name.

Before speaking:
Estimate travel times from all agent positions to each place.
Propose the most optimal location, or support/comment on existing suggestions.
If discussion is dragging, compromise — accept the most agreed-upon option.

Mention places as <Place Name> — exact match required.

Keep speech under 100 words, 1–3 sentences. Be clear, polite, and action-oriented.

Never endlessly propose new locations. If 2+ rounds have passed without convergence, actively support or finalize the leading candidate.

You must speak at least once before using "decide" — ensure others know you’re about to finalize.

Now generate your response. Output ONLY the JSON block wrapped in json. No explanations. No extra characters.