$TaskDescription$

Now the agent is in a group discussion. Based on prior analysis, the agent has decided to retrieve some information. You need use your ability of spatial reasoning to generate a retrieval plan. You also have access to a hierarchical scene graph built, representing the city you are in. In your plan, you can query the navigation app **at most once**.

—

**Input:**

Current Time:
$CurrentTime$

- Agent’s Name: $SelfName$

- Agent's position: $SelfPose$

- Intent:
$QueryIntent$

Note that the team only share the conversation history, instead of all infos.

—

**Output Format:**

Return a JSON object representing the operation chain:

```json
[
    "action 1",
    "action 2",
    ...
]
```

—

You must select from the available operations:

{
    "type": "query_app",
    "query_type": "qeury_nearby",
    "coordinate": ["x-coordinate", "y-coordinate"] # this action will trigger a query send to the navigation app.
    "radius": "r" # a float number, representing the radius of the query.
}

{
    "type": "query_app",
    "query_type": "qeury_place",
    "place": "<Place name>" # this action will trigger a query send to the navigation app.
}

{
    "type": "query_app",
    "query_type": "query_route",
    "place": "<Tartget place name>" # this action will trigger a spatial reasoning chain to handle the query procedure.
}

{
    "type": "parse",
    "source": "conversation_content" # this action will trigger a spatial reasoning to reconstruct a subgraph from the given conversation content and merge it into the global scene graph.
}

{
    "type": "overlook",
    "area": [x_min, y_min, x_max, y_max] # this action will trigger a multimodal model to reconstruct a subgraph based on the aerial view of the given area and merge it into the global scene graph. The area shouldn't be large than 100x100. 
}

—

This output will be directly executed by the system to trigger navigation queries and spatial reasoning. Be precise — any deviation may cause parsing or execution failure. Be concise - the cost of reasoning and queries can be expensive.

Please generate your response now.