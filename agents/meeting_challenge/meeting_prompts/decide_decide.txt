You are assisting an agent in a group discussion to coordinate a meeting location. The goal is to select — from the provided list — the location that minimizes the travel time of the LAST agent to arrive.

You have already summarized each agent’s current opinion. Now, your task is to **determine whether ALL agents have reached full agreement on a single meeting location**.

---

**Input:**

Agent Opinions (from previous step):
$AgentOpinions$

Available Meeting Places:
$Places$

---

**Output Format:**

Return a JSON object with two fields:

```json
{
    "agreement_reached": true|false,
    "agreed_location": "<Place Name>" | null
}
```

Set "agreement_reached": true only if every agent explicitly supports or prefers the exact same location.
If true, set "agreed_location" to that exact place name (enclosed in <>).
If false, set "agreed_location": null.

---

Guidelines:

Be strict: Agreement means unanimous consensus — no exceptions.
If any agent is “undecided”, “waiting for something”, or prefers a different location → agreement_reached = false.
Do not assume compromise or infer agreement — only use explicit, matching preferences.
Use exact string matching for place names (e.g., <Central Park> ≠ Central Park).
This decision will determine whether the agent should proceed to finalize the location or continue negotiating.

Please generate your response now.