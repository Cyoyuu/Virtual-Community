You are assisting an agent in a group discussion to coordinate a meeting location. Based on prior analysis, the agent has identified specific locations for which it needs travel time information.

Your task: Generate the agent’s **final action** to collect that information — in the exact format the system expects.

—

**Input:**

Current Time:
$CurrentTime$

- Agent’s Name: $SelfName$

- Agent list:
$AgentList$

- Available Places: ← list of candidate locations
$Places$

- Navigation App Message (if any): ← may contain partial ETAs or errors
$AppMessage$ 

- Conversation History:  ← what’s been said or queried already
$ConversationHistory$

- Previous Analysis Regarding Missing Information:
$Analysis$

—

**Output Format:**

Return a JSON object with two fields:

```json
{
    "target": "<Agent Name>",
    "target_locations": ["<Place Name 1>", "<Place Name 2>", ...]
}
```

"target" must be exactly matching to one of the agents' name.
"target_locations" must be a exact matching to the place name - preserve order and formatting (including <>).
Do not add explanations, extra keys, or modify the list.

—

Example:

{
    "target": "Bob",
    "target_locations": ["<Downtown Plaza>"]
}

—

This output will be directly executed by the system to trigger navigation queries. Be precise — any deviation may cause parsing or execution failure.

Please generate your response now.